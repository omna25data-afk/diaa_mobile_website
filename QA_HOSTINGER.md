# نتائج التحقق المحلي لنسخة Hostinger

تم تشغيل نسخة الإنتاج محليًا عبر `pnpm start` بعد تنفيذ البناء، واستخدمت قاعدة البيانات المتاحة في بيئة الاختبار للتحقق من استجابة التطبيق. حمّلت الصفحة الرئيسية والواجهة العامة بنجاح، وظهرت الصفحة بمحتواها العربي وتخطيطها المتجاوب.

أظهر الفحص أن بعض البيانات الحالية ما زالت تحتوي على مسارات وسائط قديمة تبدأ بـ `/manus-storage/`. أضيف تحويل موحّد لهذه المسارات إلى أصول محلية داخل `/media` في نسخة النقل، لتستمر الصور بالظهور حتى عند نقل قاعدة بيانات قائمة. أما قاعدة جديدة على Hostinger فسيضيف `pnpm db:seed` مباشرةً مسارات محلية متوافقة.

تم فتح بوابة `/admin` في نسخة الإنتاج المنقولة. تتطلب البوابة ضبط `ADMIN_EMAIL` و`ADMIN_PASSWORD` و`JWT_SECRET` في بيئة الاستضافة؛ وهي قيم اختبار مؤقتة محليًا ولا تُحفظ في المستودع.

أثناء الفحص عبر المعاينة، ظهرت مساحة التطبيق فارغة في لقطة التحميل الأولى بعد التنقل، ثم ظهر المحتوى عند اكتمال استدعاء بيانات الموقع. لذلك ينبغي تقييم الصفحة بعد اكتمال التحميل، لا من اللقطة الأولى فقط.

بعد تطبيق تحويل الوسائط، ظهرت الصفحة الرئيسية في المعاينة بمسارات `/media` المحلية للشعار وصور التطبيق، بدل مسارات التخزين المدمجة القديمة. كما ظهر نموذج دخول المدير المستقل في المسار `/admin` مع حقلي البريد الإلكتروني وكلمة المرور.

تم إدخال بيانات اختبار مؤقتة متوافقة مع متغيرات بيئة التشغيل، ثم نجح تسجيل الدخول وظهرت قشرة لوحة الإدارة باسم «مدير ضياء موبايل». يؤكد ذلك إنشاء ملف الجلسة الجديد وحماية مساحة الإدارة بعيدًا عن مصادقة البيئة السابقة.

أظهر `drizzle-kit check` اتساق مخطط قاعدة البيانات وملفات الترحيل. كما نُفّذ أمر البذر بأمان على اتصال قاعدة متاح وتوقف دون تعديل عند اكتشاف الإعدادات الرئيسية الموجودة، وهي آلية الحماية المطبقة قبل إدراج البيانات. وفحص خادم الإنتاج الأصول المحلية الأربع بحالة HTTP 200.

أعيد تحميل لوحة الإدارة من الخادم بعد تحديث التطبيع للتأكد من عدم الاعتماد على بيانات React Query المخزنة مؤقتًا، ثم جرى انتظار استجابة بيانات الإدارة الجديدة قبل اعتماد النتيجة.

بعد اكتمال إعادة التحميل، ظهرت حقول رابط الشعار وصورة الواجهة داخل لوحة الإدارة بقيم `/media/diaa-app-icon.jpg` و`/media/diaa-app-promo-dark.jpg`. وبذلك يغطي التطبيع الموقع العام ولوحة الإدارة والبيانات الراجعة من الخادم.
